---
title: "Getting Started"
---

# Getting Started

Install the package with `npm install experiment-api`. Then import and configure:

```typescript
import { createClient } from "experiment-api";
const client = createClient({ apiKey: "your-key" });
```
